# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'user_target_xcconfig' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'GoogleMobileAdsMediationFacebook', '6.3.1.0'
  pod 'GoogleMobileAdsMediationMoPub', '5.16.2.0'

  # Pods for user_target_xcconfig

  target 'user_target_xcconfigTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'user_target_xcconfigUITests' do
    # Pods for testing
  end

end
